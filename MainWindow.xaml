<Window x:Class="WpfApp13.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp13"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
    </Window.Resources>
    <Grid>
        <Grid x:Name="Nav">
            <!--<local:Page1>
                <local:Page1.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleX="0.5" ScaleY="{Binding RelativeSource={RelativeSource Mode=Self},Path=ScaleX}"></ScaleTransform>
                    </TransformGroup>
                </local:Page1.RenderTransform>
            </local:Page1>-->
        </Grid>
        <StackPanel Orientation="Horizontal"  VerticalAlignment="Top">
            <Button Height="20" Click="Button_Click">page1</Button>
            <Button Height="20" Click="Button_Click">page2</Button>
        </StackPanel>
    </Grid>
</Window>
